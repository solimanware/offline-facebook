import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smsmware.offlinefacebook',
  appName: 'offline-facebook',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
